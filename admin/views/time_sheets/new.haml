%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:time_sheets, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:time_sheets, :new)

.tabs-content
  = form_for :time_sheet, url(:time_sheets, :create), :class => 'form-horizontal' do |f|
    =partial 'time_sheets/form', :locals => { :f => f }
